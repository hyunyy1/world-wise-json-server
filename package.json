{
  "name": "worldwise-json-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch cities.json --port 8000"
  },
  "author": "hyunyy1",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.22"
  }
}
